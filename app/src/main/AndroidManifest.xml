<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="the.team.shotgun">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LandingActivity" />
        <activity android:name=".login.RegistrationActivity" />
        <activity android:name=".carpool.RequestActivity" />
        <activity android:name=".driving.survey.DrivingChecklistActivity" />
        <activity android:name=".driving.SelectDrivingDestinationActivity" />
        <activity android:name=".driving.OfferCarpoolActivity" />
        <activity android:name=".driving.garage.FindGarageActivity" />
        <activity android:name=".carpool.PossibleRidersActivity"></activity>
    </application>

</manifest>